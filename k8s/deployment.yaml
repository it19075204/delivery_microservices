apiVersion: v1
kind: Pod
metadata:
  name: server-pod-deliveryservice
  labels:
    name: server-pod-deliveryservice
    app: cluster-1

spec:
  containers:
    - name: server-pod-deliveryservice
      image: dilmika4172/cluster-1
      ports:
        - containerPort: 5000
